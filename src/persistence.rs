use crate::{HEIGHT_BINS, NUMBER_OF_ACTIONS, VELOCITY_BINS};

pub(crate) fn save(table: &[[[f32; NUMBER_OF_ACTIONS]; VELOCITY_BINS]; HEIGHT_BINS]) {
    todo!()
}

pub(crate) fn load() -> Option<[[[f32; NUMBER_OF_ACTIONS]; VELOCITY_BINS]; HEIGHT_BINS]> {
    todo!()
}
